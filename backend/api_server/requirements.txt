# Dev-only local API server dependencies. Not part of the backend package's
# own dependencies (backend/functions has none beyond the stdlib) — these
# are only needed to run backend/api_server/main.py locally.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.53.0,<1.0
firebase-admin>=6.5,<7.0
libsql==0.1.11
